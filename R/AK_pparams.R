#' Parameters for `AK_model()` in `pparams()` form
#'
#' @format ## `AK_pparams`
#' A list of 1 named vector and 1 matrix.
#'
#' \describe{
#'   \item{shared}{Only contains the value for `mu`, which was treated as fixed}
#'   \item{specific}{Matrix of unit-specific parameter estimates. Rows are
#'   parameter names, columns are unit names.}
#' }
"AK_pparams"
